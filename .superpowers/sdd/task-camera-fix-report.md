Fixed the Phase 2 camera preset review item.

- Added camera presets to all five scenario JSON files, targeting the relevant starter district areas from node positions.
- Changed `CityScene.setCameraPreset` to ease camera position and OrbitControls target over 800ms during render.
- Verified with `npm test` and `npm run build -w @lcc/web`.
